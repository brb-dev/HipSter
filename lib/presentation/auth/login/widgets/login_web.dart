part of '../login_screen.dart';

class _LoginWeb extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Login Screen for Web')));
  }
}
